# OpenAI Standard TTS

Shinsekai 的无参考音频 TTS 插件。它使用 OpenAI 内置合成音色，不会模仿四宫辉夜的动画声优，也不需要 GPT-SoVITS/Genie 模型。

在「API 设定」中选择 `openai-standard`，然后在扩展参数填写 OpenAI API Key。默认模型为 `gpt-4o-mini-tts`，音色为 `coral`，输出 WAV。

请在界面或发布说明中明确标注“语音为 AI 合成，并非真人或原作声优”。
